function calculate(){
    let num =document.getElementById('pi').Value
    num=parseInt(num)&&Number(num)
    if(!isNaN(num)){
        document.getElementById('pi').Value=pi(num)
    }
}
function factorial(n){
    let sum=1
    for(let i=1;i<=n;++i)
    sum * i
}
return sum

function pi(num){
    let sum =0
    for (let i=0;i<=num;++i){
        let dividend = factorial()
        let dividend=oddFactorial()
        sum
    }
    return sum*2
}
calculate()